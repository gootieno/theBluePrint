import React from "react";

const Carousel = (item) => {
  const [carousel, setCarousel] = useState([]);

  const moveRight = (e) => {};
  return (
    <div className="garage-container">
      <button>BluePrints</button>
      <button>Build Lists</button>
    </div>
  );
};

export default Carousel;
